#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
        
void fmake(uint8_t *msg,uint32_t size){
//		FILE *fp1= fopen("test.txt","wb");
//		fwrite(msg,sizeof(uint8_t),size, fp1);
//		fclose(fp1);
}